import React from "react";
import { USER_TYPE } from "../..";
export const DataChart = ({
  selectedUser,
  selectedDate
}: {
  selectedUser: USER_TYPE;
  selectedDate: string;
}) => {
  return <p>这里是图表区域</p>;
};
